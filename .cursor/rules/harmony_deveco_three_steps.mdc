---
description: After editing frontend/harmonyApp, sync + build + hdc install + start without asking; user expects fresh HAP on device every time
globs: frontend/harmonyApp/**
alwaysApply: false
---

# Harmony：改完代码后默认跑通「编译 + 安装」

只要本轮对话里**修改了** `frontend/harmonyApp/` 下源码、资源或配置，在**结束本轮前**须在仓库内跑完下面流程，**不要**问用户「要不要 install」——默认**都要**（与用户对齐的习惯）。

## 环境

- `export DEVECO_SDK_HOME=/Applications/DevEco-Studio.app/Contents/sdk`
- 工作目录：`frontend/harmonyApp`

## 推荐一键顺序（与当前工程产物一致）

1. **依赖（按需，有变更时）**  
   `"/Applications/DevEco-Studio.app/Contents/tools/ohpm/bin/ohpm" install`

2. **Sync**  
   `"/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw" --sync -p product=default --analyze=normal --parallel --incremental --daemon`

3. **编 entry HAP**（当前产出为 unsigned）  
   `"/Applications/DevEco-Studio.app/Contents/tools/hvigor/bin/hvigorw" assembleHap --mode module -p module=entry@default -p product=default --analyze=normal --parallel --incremental --daemon`

4. **安装并拉起**（在装包前可先 `hdc list targets`；多设备时用 `-t <序列号>`，常见模拟器为 `127.0.0.1:5555`）  
   - HAP：`frontend/harmonyApp/entry/build/default/outputs/default/entry-default-unsigned.hap`  
   - `hdc -t <target> install -r "<上述绝对路径>"`  
   - `hdc -t <target> shell aa start -a EntryAbility -b com.silk.harmony`

若某一步失败，在本轮内 retry 或修到通过为止。

**禁止**在日常迭代里加 `hdc uninstall`（会清空沙箱与用户偏好）。仅在需要净机重装时再手动 uninstall。
